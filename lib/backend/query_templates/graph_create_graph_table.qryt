CREATE TABLE graph(
	id         int PRIMARY KEY,
	computed   map<text, frozen<map  <text, frozen <trueno_tuple>>>>,
	attributes map<text, frozen<trueno_tuple>>,
	meta       map<text, frozen<trueno_tuple>>
);
